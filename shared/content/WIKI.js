/**
 * REGEXHELPER — WIKI (конструктор)
 * @file shared/content/WIKI.js
 */

export const WIKI_HTML = `
<div class="wiki-content wiki-content-compact" id="wiki-top">
  <nav class="wiki-nav" aria-label="Оглавление">
    <strong>Содержание</strong>
    <ul>
      <li><a href="#wiki-quickstart-hero">Быстрый старт</a></li>
      <li><a href="#wiki-auto">⚡ Автоматические преобразования</a></li>
      <li><a href="#wiki-intro">1. Что делает конструктор</a></li>
      <li><a href="#wiki-simple">2. Простые триггеры</a></li>
      <li><a href="#wiki-linked">3. Связанные триггеры (конструктор)</a></li>
      <li><a href="#wiki-import">4. Импорт паттернов</a></li>
      <li><a href="#wiki-params">5. Параметры</a></li>
      <li><a href="#wiki-result">6. Результат и история</a></li>
      <li><a href="#wiki-tools">7. Другие инструменты</a></li>
      <li><a href="#wiki-syntax">8. Шпаргалка по regex</a></li>
    </ul>
  </nav>

  <section id="wiki-quickstart-hero">
    <h2>Быстрый старт (30 секунд)</h2>
    <ol>
      <li><strong>Введите слова</strong> в поле «Простые триггеры» — по одному на строку.</li>
      <li><strong>Нажмите «Конвертировать»</strong>.</li>
      <li><strong>Скопируйте результат</strong> кнопкой «Копировать».</li>
    </ol>
    <p>Если нужно найти <strong>несколько слов или словосочетаний рядом</strong> (например, «военный» и «дрон» вместе) — переключитесь на <strong>«Связанные триггеры»</strong>.</p>
  </section>

  <section id="wiki-auto" class="wiki-callout wiki-callout-auto">
  <h2>⚡ Автоматические преобразования</h2>
  <p>Конструктор автоматически применяет следующие правила к каждому триггеру <strong>без участия пользователя</strong>:</p>
    <table class="wiki-syntax-table wiki-auto-table">
      <tr>
        <td><strong>ё → [её]</strong></td>
        <td>Буква «ё» заменяется на класс символов, чтобы найти и «ё», и «е».<br>
        <code>актёр</code> → <code>акт[её]р</code></td>
      </tr>
      <tr>
        <td><strong>ь → [ьъ]</strong></td>
        <td>Мягкий знак заменяется на класс, учитывающий опечатки.<br>
        <code>мазь</code> → <code>маз[ьъ]</code></td>
      </tr>
      <tr>
        <td><strong>ъ → [ъь]</strong></td>
        <td>Твёрдый знак заменяется на класс.<br>
        <code>подъезд</code> → <code>под[ъь]езд</code></td>
      </tr>
      <tr>
        <td><strong>Удвоенные буквы</strong></td>
        <td>Вторая из двух одинаковых подряд букв становится опциональной — учитывает частые опечатки.<br>
        <code>аллея</code> → <code>алл?ея</code><br>
        <code>колонна</code> → <code>колонн?а</code><br>
        <code>многообразие</code> → <code>многоо?бразие</code></td>
      </tr>
    </table>
    <p class="wiki-auto-note">Эти преобразования нельзя отключить — они встроены в логику сервиса и повышают точность поиска.</p>
  </section>

  <section id="wiki-intro">
    <h2>1. Что делает конструктор</h2>
    <p>
      Конструктор объединяет три режима работы с регулярными выражениями:
      <strong>Связанные триггеры</strong>, <strong>Простые триггеры</strong> и <strong>Ручной редактор</strong>.
      Вместо ручного набора <code>дрон|беспилотник|квадрокоптер</code> вы вводите слова через интерфейс
      или работаете с готовым regex в редакторе — конструктор собирает и помогает доработать валидный паттерн.
    </p>
    <ul>
      <li><strong>Новичок</strong> — можно не знать синтаксис regex, достаточно понимать, что вы хотите найти.</li>
      <li><strong>Продвинутый</strong> — удобно собирать и редактировать сложные паттерны быстрее, переключаясь между режимами.</li>
    </ul>
  </section>

  <section id="wiki-simple">
    <h2>2. Простые триггеры</h2>
    <p>Используйте, когда нужно найти <strong>любое из нескольких слов</strong>.</p>
    
    <h3>Как использовать</h3>
    <ol>
      <li>Переключитесь на панель «Простые триггеры» (клик по заголовку).</li>
      <li>Введите слова — каждое с новой строки.</li>
      <li>Выберите триггеры и примените параметры (при необходимости).</li>
      <li>Нажмите «Конвертировать».</li>
    </ol>

    <div class="wiki-example">
      <div class="wiki-example-title">Пример</div>
      <p><strong>Ввод:</strong></p>
      <pre><code>дрон
беспилотник
бпла</code></pre>
      <p><strong>Результат:</strong> <code>(дрон|беспилотник|бпла)</code></p>
    </div>

    <h3>Параметры для триггеров</h3>
    <p>Справа от поля ввода находится панель параметров. Выберите триггер (клик) и примените нужные параметры:</p>
    <ul>
      <li><strong>Лат/Кир</strong> — заменяет похожие буквы: <code>a</code> → <code>[аa]</code>, <code>o</code> → <code>[оo]</code></li>
      <li><strong>Транслитерация</strong> — учитывает латиницу и кириллицу: <code>king</code> → <code>[kк][iи][nн][gг]</code></li>
      <li><strong>Склонения</strong> — добавляет окончания существительных (подробнее ниже)</li>
      <li><strong>Опциональные символы (?)</strong> — делает выбранные символы необязательными: <code>пассивный</code> → <code>пасс?ивный</code></li>
      <li><strong>Любой символ (\\w)</strong> — добавляет символы в конец: <code>доставк</code> → <code>доставк\\w{1,3}</code></li>
      <li><strong>Границы слова (\\b)</strong> — ищет целое слово; выбор: в начале, в конце или с обеих сторон</li>
      <li><strong>Пробел после (\\s)</strong> — требует пробел после слова</li>
    </ul>
    <p>Под каждым триггером появляются бейджи применённых параметров.</p>
  </section>

  <section id="wiki-linked">
    <h2>3. Связанные триггеры (визуальный конструктор)</h2>
    <p>Используйте, когда нужно найти <strong>несколько слов, словосочетаний или сложные конструкции</strong> с определёнными связями между ними.</p>

    <h3>Интерфейс конструктора</h3>
    <ul>
      <li><strong>Слева</strong> — интерактивное поле с триггерами-чипами</li>
      <li><strong>Справа</strong> — панель параметров и соединителей</li>
    </ul>

    <h3>Как использовать</h3>
    <ol>
      <li>Переключитесь на «Связанные триггеры».</li>
      <li>Нажмите <strong>«+ Триггер»</strong> — появится новый чип.</li>
      <li>Введите слово (чип сразу в режиме редактирования).</li>
      <li>Добавьте ещё триггеры.</li>
      <li>Настройте <strong>соединители</strong> между триггерами (см. ниже).</li>
      <li>Примените <strong>параметры</strong> к отдельным триггерам (при необходимости).</li>
      <li>Нажмите «Конвертировать».</li>
    </ol>

    <h3>Соединители между триггерами</h3>
    <p>Соединитель определяет, что может быть между словами. Выберите триггер и нажмите на соединитель в панели справа:</p>
    <ul>
      <li><strong>Альтернация <code>|</code></strong> — «или»: <code>дрон|беспилотник</code></li>
      <li><strong>Любое расстояние <code>[\\s\\S]+</code></strong> — любые символы между словами, включая переносы строк</li>
      <li><strong>В пределах абзаца <code>.+</code></strong> — любые символы, но без переносов строк</li>
      <li><strong>В пределах строки <code>[^\\n]+</code></strong> — только в одной строке</li>
      <li><strong>Своё значение <code>.{min,max}</code></strong> — точное количество символов между словами</li>
    </ul>
    <p>Бейдж соединителя появляется справа от чипа.</p>

    <h3>Группировка</h3>
    <p>Группа объединяет несколько элементов в скобки <code>()</code>.</p>
    <ol>
      <li>Выберите несколько смежных триггеров (<strong>Ctrl+клик</strong> или <strong>Shift+клик</strong>).</li>
      <li>Нажмите <strong>«Сгруппировать ( )»</strong>.</li>
    </ol>
    <p>Группы можно вкладывать друг в друга (неограниченно). Каждый уровень вложенности выделяется своим цветом рамки.</p>

    <h3>Действия с группой</h3>
    <p>Выберите группу (клик по рамке) — появится панель «Действия с группой»:</p>
    <ul>
      <li><strong>Дублировать</strong> — создаёт копию группы со всеми параметрами</li>
      <li><strong>Инвертировать</strong> — меняет порядок элементов на обратный</li>
      <li><strong>Разгруппировать</strong> — убирает скобки, оставляя элементы</li>
    </ul>

    <h3>Редактирование и удаление</h3>
    <ul>
      <li><strong>Двойной клик</strong> по чипу — режим редактирования</li>
      <li><strong>Кнопка ×</strong> на чипе — удалить триггер</li>
      <li><strong>Перетаскивание</strong> — меняйте порядок элементов мышью</li>
    </ul>

    <div class="wiki-example">
      <div class="wiki-example-title">Пример: поиск двух слов рядом</div>
      <p><strong>Задача:</strong> найти «выкуп» и «дорого» рядом (до 7 символов между ними).</p>
      <ol>
        <li>Добавьте триггер «выкуп».</li>
        <li>Выберите его, нажмите «Своё значение» в соединителях.</li>
        <li>Укажите мин: 1, макс: 7.</li>
        <li>Добавьте триггер «дорого».</li>
        <li>Конвертируйте.</li>
      </ol>
      <p><strong>Результат:</strong> <code>выкуп.{1,7}дорого</code></p>
    </div>
  </section>

  <section id="wiki-import">
    <h2>4. Импорт паттернов</h2>
    <p>Если у вас уже есть готовое регулярное выражение, его можно импортировать в конструктор для дальнейшего редактирования.</p>

    <h3>Как импортировать</h3>
    <ol>
      <li>Переключитесь на «Связанные триггеры».</li>
      <li>Нажмите <strong>«Импорт паттерна»</strong> в шапке панели.</li>
      <li>Вставьте regex в текстовое поле.</li>
      <li>Проверьте анализ — ошибки подсвечиваются прямо в тексте.</li>
      <li>Нажмите <strong>«Импорт»</strong>.</li>
    </ol>

    <h3>Что распознаётся автоматически</h3>
    <table class="wiki-syntax-table">
      <tr>
        <td><code>[её]</code> → <code>ё</code></td>
        <td>Автозамена буквы «ё»</td>
      </tr>
      <tr>
        <td><code>[ьъ]</code> → <code>ь</code></td>
        <td>Автозамена мягкого знака</td>
      </tr>
      <tr>
        <td><code>оо?</code> → <code>оо</code></td>
        <td>Автоудвоение (вторая буква та же)</td>
      </tr>
      <tr>
        <td><code>но?к</code> → <code>нок</code></td>
        <td>Опциональный символ «о» (буквы разные)</td>
      </tr>
      <tr>
        <td><code>[аa]</code> → <code>а</code></td>
        <td>Параметр «Лат/Кир»</td>
      </tr>
      <tr>
        <td><code>[рr][оo][бb]</code> → <code>роб</code></td>
        <td>Параметр «Транслитерация»</td>
      </tr>
      <tr>
        <td><code>\\b...\\b</code></td>
        <td>Параметр «Границы слова»</td>
      </tr>
      <tr>
        <td><code>слово\\s</code></td>
        <td>Параметр «Пробел после»</td>
      </tr>
      <tr>
        <td><code>слово\\w{1,3}</code></td>
        <td>Параметр «Любой символ» (wildcard)</td>
      </tr>
      <tr>
        <td><code>[\\s\\S]+</code></td>
        <td>Соединитель «Любое расстояние»</td>
      </tr>
      <tr>
        <td><code>.+</code></td>
        <td>Соединитель «В пределах абзаца»</td>
      </tr>
      <tr>
        <td><code>.{0,20}</code></td>
        <td>Соединитель «Своё значение»</td>
      </tr>
    </table>

    <h3>Предупреждения и ошибки</h3>
    <p>Если парсер не распознаёт часть паттерна, она <strong>подсвечивается красным</strong> прямо в поле ввода. Под полем появляется список предупреждений.</p>
    <p>Импорт разрешён даже при наличии предупреждений — вы можете исправить проблемные места в конструкторе после импорта.</p>

    <div class="wiki-callout wiki-callout-warning">
      <strong>Не поддерживается:</strong>
      <ul style="margin-bottom: 0;">
        <li>Lookahead/Lookbehind: <code>(?=...)</code>, <code>(?!...)</code>, <code>(?&lt;=...)</code>, <code>(?&lt;!...)</code></li>
        <li>Backreferences: <code>\\1</code>, <code>\\2</code>, ...</li>
        <li>Именованные группы: <code>(?&lt;name&gt;...)</code></li>
        <li>Формат <code>/pattern/flags</code> — вставляйте только паттерн без слешей</li>
      </ul>
    </div>
  </section>

  <section id="wiki-params">
    <h2>5. Параметры</h2>

    <h3>Лат/Кир</h3>
    <p>Заменяет визуально похожие буквы на класс символов:</p>
    <ul>
      <li><code>a</code> → <code>[аa]</code> (русская и латинская)</li>
      <li><code>o</code> → <code>[оo]</code></li>
      <li><code>e</code> → <code>[еe]</code></li>
    </ul>
    <p>Полезно, когда текст может содержать смешанные раскладки.</p>

    <h3>Транслитерация</h3>
    <p>Преобразует каждую букву в класс символов для поиска латиницы и кириллицы:</p>
    <ul>
      <li><code>king</code> → <code>[kк][iи][nн][gг]</code></li>
      <li><code>привет</code> → <code>[пp][рr][иi][вv][еe][тt]</code></li>
    </ul>
    <p>Найдёт слово независимо от раскладки: <code>roblox</code>, <code>роблокс</code> и смешанные варианты.</p>

    <h3>Склонения</h3>
    <p>Добавляет падежные формы русских слов. <strong>Два режима:</strong></p>
    <ul>
      <li><strong>Автоматически</strong> — конструктор сам определяет возможные формы</li>
      <li><strong>Точные окончания</strong> — вы указываете корень и конкретные окончания вручную</li>
    </ul>
    
    <h4>Авто-режим поддерживает:</h4>
    <table class="wiki-syntax-table">
      <tr>
        <td><strong>Существительные (ед.ч.)</strong></td>
        <td><code>дом</code> → <code>дом(ами|ом|ов|ам|ах|а|у|е|...)</code><br>
        Все 6 падежей + формы множественного числа</td>
      </tr>
      <tr>
        <td><strong>Существительные (мн.ч.)</strong></td>
        <td><code>книги</code> → <code>книг(и|ам|ами|ах|ой|а|у|...)</code><br>
        Определяет мн. число и склоняет по падежам</td>
      </tr>
      <tr>
        <td><strong>Прилагательные</strong></td>
        <td><code>красивый</code> → <code>красив(ый|ого|ому|ым|ом|ая|ой|ую|ое|ые|ых|ыми|...)</code><br>
        Все рода, числа и падежи</td>
      </tr>
      <tr>
        <td><strong>Причастия</strong></td>
        <td><code>написанный</code> → <code>написанн(ый|ого|ому|ым|ом|ая|ой|ую|ое|ые|ых|ыми|...)</code><br>
        Склоняются как прилагательные</td>
      </tr>
    </table>

    <h4>Точные окончания (ручной режим)</h4>
    <p>Укажите корень слова и нужные окончания:</p>
    <ul>
      <li>Корень: <code>книг</code>, окончания: <code>а, и, у, ой</code> → <code>книг(а|и|у|ой)</code></li>
      <li>Корень: <code>доставк</code>, окончания: <code>а, и, ой, ами</code> → <code>доставк(а|и|ой|ами)</code></li>
    </ul>
    
    <div class="wiki-callout wiki-callout-warning">
      <strong>Не поддерживаются в авто-режиме:</strong>
      <ul style="margin-bottom: 0;">
        <li><strong>Глаголы</strong> (спряжение): читать, пишешь, делает — используйте <code>\\w</code> или точные окончания</li>
        <li><strong>Краткие прилагательные</strong>: красив, красива, красиво — используйте точные окончания</li>
      </ul>
    </div>
    
    <p>Режим «Точные окончания» полезен, когда автоматика даёт лишние формы, нужны только специфические окончания, или для слов, которые не поддерживаются в авто-режиме.</p>

    <h3>Любой символ (\\w)</h3>
    <p>Добавляет wildcard в конец слова. <strong>Два режима:</strong></p>
    <ul>
      <li><strong>Авто</strong> — один или более символов: <code>слов\\w+</code></li>
      <li><strong>Диапазон</strong> — указываете мин и макс: <code>слов\\w{1,3}</code></li>
    </ul>
    <p>Полезно для поиска слов с разными окончаниями: <code>доставк\\w{1,3}</code> найдёт «доставка», «доставки», «доставкой».</p>

    <h3>Границы слова (\\b)</h3>
    <p>Добавляет <code>\\b</code> — границу слова. При клике открывается popup с выбором позиции:</p>
    <ul>
      <li><strong>В начале</strong> — <code>\\bкот</code> (найдёт «кот», «котёнок», но не «который»)</li>
      <li><strong>В конце</strong> — <code>кот\\b</code> (найдёт «кот», «бойкот», но не «котёнок»)</li>
      <li><strong>С обеих сторон</strong> — <code>\\bкот\\b</code> (найдёт только «кот»)</li>
    </ul>
    <p><strong>При склонениях или \\w:</strong> доступен только режим «В начале», т.к. конец слова меняется.</p>

    <h3>Пробел после (\\s)</h3>
    <p>Требует обязательный пробел (или табуляцию, перенос строки) после слова:</p>
    <ul>
      <li><code>слово</code> → <code>слово\\s</code></li>
    </ul>

    <div class="wiki-callout wiki-callout-info">
      <strong>Совместимость параметров:</strong> некоторые параметры нельзя использовать вместе.
      При выборе конфликтующего параметра предыдущий автоматически снимается.
    </div>
  </section>

  <section id="wiki-result">
    <h2>6. Результат и история</h2>

    <h3>Панель результата</h3>
    <ul>
      <li><strong>Конвертировать</strong> — собирает regex из триггеров</li>
      <li><strong>Копировать</strong> — копирует результат в буфер</li>
      <li><strong>В тестер</strong> — отправляет regex в тестер для проверки</li>
      <li><strong>В визуализатор</strong> — отправляет regex в визуализатор и открывает <strong>новую вкладку диаграммы</strong>, не затирая уже построенные</li>
      <li><strong>В редактор</strong> — отправляет regex в ручной редактор для ручной доработки и проверки синтаксиса</li>
      <li><strong>Очистить</strong> — очищает поле результата</li>
    </ul>

    <h3>История</h3>
    <p>Все конвертации сохраняются автоматически (до 300 записей, 7 дней). Записи из ручного редактора тоже попадают в общую историю.</p>
    <ul>
      <li>Карточки показывают дату, <strong>тип</strong> (Простые, Связанные или <strong>Из редактора</strong>), триггеры (для конвертаций) или только результат (для записей «Из редактора»)</li>
      <li><strong>«В редактор»</strong> — на каждой карточке: подставляет regex из записи в ручной редактор (полная замена), прокручивает страницу к редактору; в модалке «Вся история» после клика модалка закрывается</li>
      <li>Кнопка «Показать всё» открывает полную историю</li>
      <li>Можно копировать и удалять записи</li>
    </ul>

    <h3>Кнопка «Сбросить всё»</h3>
    <p>В шапке сайта — сбрасывает все панели, параметры, результаты. История сохраняется.</p>
  </section>

  <section id="wiki-tools">
    <h2>7. Другие инструменты</h2>

    <h3>Ручной редактор (режим конструктора)</h3>
    <p>
      Режим конструктора, который позволяет <strong>вручную редактировать регулярное выражение</strong>, используя справа готовые кнопки‑подсказки для
      вставки параметров и соединителей: <code>\\w</code>, <code>\\b</code>, <code>\\s</code>, <code>|</code>, <code>[\\s\\S]+</code>, <code>.{0,10}</code> и т.д.,
      а также отдельную кнопку <strong>Lookbehind</strong> с готовыми пресетами для контекста слева (например, «нет слова <code>не</code> перед словом», «символ сразу после пробела», «символ после цифры»).
      Удобен, когда нужно доработать результат триггеров или собрать regex с нуля, но с подсказками по синтаксису.
    </p>
    <p>
      Переключиться в этот режим можно в шапке конструктора по табу «Ручной редактор».
    </p>

    <div class="wiki-callout wiki-callout-editor-invert">
      <h4>Инвертировать выделенное</h4>
      <p>
        Кнопка <strong>«Инвертировать выделенное»</strong> в шапке редактора (рядом с подсказкой) позволяет быстро получить <strong>обратный порядок элементов</strong> regex и добавить его в конец поля через альтернацию <code>|</code>, не переписывая само выражение.
      </p>
      <p><strong>Как работает:</strong></p>
      <ul>
        <li>Выделите в поле редактора фрагмент регулярного выражения (например, цепочку с группами и соединителями).</li>
        <li>Кнопка «Инвертировать выделенное» при отсутствии выделения скрыта визуально, но место под неё в строке сохраняется — шапка не «прыгает» при появлении кнопки.</li>
        <li>Нажмите кнопку — выделенный фрагмент разбирается на элементы (группы, триггеры, соединители <code>[\\s\\S]+</code>, <code>[^\\n]+</code>, <code>.{0,10}</code> и т.д.), порядок элементов разворачивается (как «Инвертировать» у группы в режиме «Связанные триггеры» конструктора), и <strong>получившийся regex вставляется в конец</strong> текущего текста через <code>|</code>.</li>
        <li>При этом <strong>текст самих сегментов не меняется</strong>: не добавляются автозамены вроде <code>[ьъ]</code> к словам, не переставляются <code>\b</code> и скобки; инвертируются только порядок сегментов и соединители между ними.</li>
      </ul>
      <p><strong>Пример:</strong> в поле есть <code>пульт.{0,10}сч[её]тчик</code>. После инверсии в конец добавится <code>|сч[её]тчик.{0,10}пульт</code> — без появления <code>пул[ьъ]т</code> и других автодобавлений.</p>
      <p>Если выделенный текст не удаётся разобрать как regex, показывается сообщение с рекомендацией воспользоваться режимами триггеров конструктора.</p>
    </div>

    <ul>
      <li><strong>«В редактор»</strong> в панели результата отправляет готовый regex в поле редактора (полная замена). То же действие — кнопка «В редактор» на любой карточке истории.</li>
      <li><strong>«Сохранить в историю»</strong> — сохраняет текущее выражение из редактора в общую историю (только при непустом поле). В истории такая запись отображается с бейджем «Из редактора», без блока триггеров.</li>
      <li>Кнопки справа <strong>вставляют</strong> фрагменты в позицию курсора (не применяются автоматически, а помогают не набирать символы вручную).</li>
      <li>Кнопка «Проверить» включает подсветку синтаксических ошибок прямо в поле: проблемный символ подсвечивается, под полем появляется подсказка. Повторное нажатие выключает подсветку.</li>
      <li>«В визуализатор» и «В тестер» — отправляют выражение в соответствующий инструмент; при переходе «В визуализатор» создаётся отдельная вкладка диаграммы, текущие вкладки не перезаписываются.</li>
    </ul>

    <h3>Тестер</h3>
    <p>
      Проверяет regex на тестовом тексте в реальном времени.
      Введите регулярные выражения и текст — совпадения подсветятся разными цветами.
      Показывает информацию о каждом совпадении и захваченных группах.
      Ошибки синтаксиса (лишние скобки, <code>|</code> в начале/конце, пустая альтернация <code>||</code>, лишняя <code>}</code>) подсвечиваются прямо в поле regex по мере ввода — видно все места ошибок сразу.
      Удобно для отладки перед использованием.
    </p>
    <p>
      В тестере есть <strong>два поля для регулярных выражений</strong>:
    </p>
    <ul>
      <li><strong>Регулярное выражение (TRUE – что ловить)</strong> — основное выражение, которое ищет совпадения.</li>
      <li><strong>Регулярное выражение (FALSE – что исключить)</strong> — дополнительное выражение-исключение (опционально).</li>
    </ul>
    <p>
      Если заполнено только поле TRUE, тестер работает как обычно. Если вы заполнили ещё и поле FALSE (и паттерн валиден), то:
    </p>
    <ul>
      <li>совпадения, которые подходят под FALSE и <strong>пересекаются по позициям</strong> с совпадениями TRUE, исключаются из результата;</li>
      <li>в тестовом тексте подсвечиваются только очищенные совпадения TRUE (после исключения FALSE);</li>
      <li>внизу отображаются две секции: <strong>«Совпадения TRUE (после исключения FALSE)»</strong> и <strong>«Совпадения FALSE (исключения)»</strong>;</li>
      <li>FALSE <strong>не работает сам по себе</strong>: если TRUE пустой или невалиден, совпадения по FALSE не считаются и не подсвечиваются.</li>
    </ul>
    <p>
      Типичный сценарий: вы собрали сложный паттерн в конструкторе (в режимах триггеров), отправили его в тестер, убедились, что он ловит нужные кейсы, а затем через поле FALSE добавили слова/фразы, которые нужно <strong>исключить</strong> из срабатывания (например, оставить «слово1» и «слово2», но игнорировать «слово3»).
    </p>
    <p>
      Справа от полей TRUE/FALSE находится компактная <strong>панель параметров и соединителей</strong> — такая же, как в ручном редакторе:
    </p>
    <ul>
      <li>кнопки <strong>(?)</strong>, <strong>(\w)</strong>, <strong>(\w{0,10})</strong>, <strong>(\b)</strong>, <strong>(\s)</strong>, <strong>|</strong>, <strong>[\s\S]+</strong>, <strong>.+</strong>, <strong>[^\n]+</strong>, <strong>.{0,10}</strong>, <strong>Lookbehind</strong>, а также <strong>[</strong> и <strong>]</strong> (квадратные скобки) вставляют соответствующие фрагменты <strong>в позицию курсора</strong> в том поле, где сейчас фокус (TRUE или FALSE); поля TRUE и FALSE пересчитываются в реальном времени при вводе в любом из них и в тестовом тексте;</li>
      <li><strong>Lookbehind</strong> открывает список типовых шаблонов для проверки контекста слева (например, «нет слова <code>не</code> перед словом», «символ сразу после пробела», «символ после цифры»). Если в поле выделен текст, он автоматически подставляется вместо плейсхолдера <code>СЛОВО</code>/<code>СИМВОЛ</code> внутри выбранного шаблона.</li>
      <li>кнопка <strong>«Инвертировать выделенное»</strong> в шапке панели тестера работает так же, как в ручном редакторе: без выделения скрыта визуально, место в шапке сохраняется; при непустом выделении в одном из полей regex инвертирует порядок элементов фрагмента <strong>без автозамен</strong> и добавляет обратный вариант в конец активного поля через <code>|</code>.</li>
    </ul>

    <h3>Визуализатор</h3>
    <p>
      Строит railroad-диаграмму из regex.
      Помогает понять структуру выражения — видно, как regex разбирает текст.
      Можно увеличивать, перемещать и экспортировать диаграмму (SVG/PNG).
      Визуализатор поддерживает <strong>вкладки</strong>: первая диаграмма заполняет стартовую вкладку «Новая диаграмма», последующие визуализации открывают новые вкладки; между вкладками можно переключаться и закрывать их по одной.
      Рядом со вкладками всегда есть компактная кнопка «+» для добавления новой пустой вкладки.
      Диаграмма вписывается по ширине области просмотра, а высота панели подстраивается под реальную длину диаграммы.
    </p>

    <h3>Текстовый помощник</h3>
    <p>
      Набор утилит для работы с текстом. Открывается кнопкой «Текстовый помощник» в навигации.
    </p>
    <ul>
      <li><strong>Столбец → Строка</strong> — объединяет строки в одну через разделитель (запятая, пробел, точка с запятой и др.).</li>
      <li><strong>Строка → Столбец</strong> — разбивает строку на отдельные строки по разделителю.</li>
      <li><strong>Удалить дубликаты</strong> — оставляет только уникальные строки, показывает отчёт о найденных дубликатах.</li>
      <li><strong>Удалить пустые строки</strong> — убирает пустые строки из текста.</li>
      <li><strong>Добавить обёртку</strong> — добавляет текст в начало и конец каждой строки (кавычки, скобки, нумерация).</li>
      <li><strong>Убрать пробелы</strong> — удаляет лишние пробелы (по краям строк, двойные пробелы).</li>
      <li><strong>Изменить регистр</strong> — преобразует текст: ВЕРХНИЙ, нижний, Каждое Слово, Первое слово, иНВЕРТИРОВАННЫЙ.</li>
    </ul>
  </section>

  <section id="wiki-syntax">
    <h2>8. Шпаргалка по regex</h2>

    <h3>Основные символы</h3>
    <table class="wiki-syntax-table">
      <tr>
        <td><code>|</code></td>
        <td><strong>«или»</strong> — альтернатива между вариантами<br>
        <code>кот|собака</code> — найдёт «кот» или «собака»</td>
      </tr>
      <tr>
        <td><code>()</code></td>
        <td><strong>Группировка</strong> — объединяет часть паттерна<br>
        <code>(чер|клуб)ника</code> — найдёт «черника» или «клубника»</td>
      </tr>
      <tr>
        <td><code>[]</code></td>
        <td><strong>Класс символов</strong> — любой один из перечисленных<br>
        <code>[аеи]</code> — найдёт «а», «е» или «и»<br>
        <code>[а-я]</code> — любая русская буква</td>
      </tr>
      <tr>
        <td><code>[^...]</code></td>
        <td><strong>Отрицание класса</strong> — любой символ, кроме перечисленных<br>
        <code>[^0-9]</code> — любой символ, кроме цифр</td>
      </tr>
    </table>

    <h3>Специальные символы</h3>
    <table class="wiki-syntax-table">
      <tr>
        <td><code>\\b</code></td>
        <td><strong>Граница слова</strong> — позиция между словом и не-словом<br>
        <code>\\bкот\\b</code> — найдёт «кот», но не «который»</td>
      </tr>
      <tr>
        <td><code>\\s</code></td>
        <td><strong>Пробельный символ</strong> — пробел, табуляция или перенос строки</td>
      </tr>
      <tr>
        <td><code>\\w</code></td>
        <td><strong>Словесный символ</strong> — буква, цифра или подчёркивание<br>
        <code>\\w+</code> — одно или более таких символов</td>
      </tr>
      <tr>
        <td><code>\\d</code></td>
        <td><strong>Цифра</strong> — символ от 0 до 9<br>
        <code>\\d{3}</code> — ровно три цифры</td>
      </tr>
      <tr>
        <td><code>.</code></td>
        <td><strong>Любой символ</strong> — кроме переноса строки \\n<br>
        <code>к.т</code> — найдёт «кот», «кат», «к1т»</td>
      </tr>
    </table>

    <h3>Квантификаторы (количество повторений)</h3>
    <table class="wiki-syntax-table">
      <tr>
        <td><code>?</code></td>
        <td><strong>0 или 1 раз</strong> — символ может быть, а может не быть<br>
        <code>цвето?к</code> — найдёт «цветок» и «цветк»<br>
        <code>пасс?ивный</code> — найдёт «пассивный» и «пасивный»</td>
      </tr>
      <tr>
        <td><code>+</code></td>
        <td><strong>1 или более раз</strong> — хотя бы один символ<br>
        <code>а+</code> — найдёт «а», «аа», «ааа»...</td>
      </tr>
      <tr>
        <td><code>*</code></td>
        <td><strong>0 или более раз</strong> — может отсутствовать или повторяться<br>
        <code>ко*т</code> — найдёт «кт», «кот», «коот»...</td>
      </tr>
      <tr>
        <td><code>{n}</code></td>
        <td><strong>Ровно n раз</strong><br>
        <code>\\d{4}</code> — ровно 4 цифры</td>
      </tr>
      <tr>
        <td><code>{n,m}</code></td>
        <td><strong>От n до m раз</strong><br>
        <code>.{1,7}</code> — от 1 до 7 любых символов</td>
      </tr>
      <tr>
        <td><code>{n,}</code></td>
        <td><strong>n или более раз</strong><br>
        <code>\\w{3,}</code> — 3 или более словесных символов</td>
      </tr>
    </table>

    <h3>Соединители из конвертера</h3>
    <table class="wiki-syntax-table">
      <tr>
        <td><code>[\\s\\S]+</code></td>
        <td><strong>Любые символы</strong> — включая переносы строк<br>
        Используется для поиска слов на любом расстоянии</td>
      </tr>
      <tr>
        <td><code>.+</code></td>
        <td><strong>В пределах абзаца</strong> — любые символы, но без переносов<br>
        Слова должны быть в одном абзаце</td>
      </tr>
      <tr>
        <td><code>[^\\n]+</code></td>
        <td><strong>В пределах строки</strong> — любые символы в одной строке<br>
        Слова должны быть на одной строке</td>
      </tr>
    </table>

    <h3>Частые паттерны</h3>
    <table class="wiki-syntax-table">
      <tr>
        <td><code>слово1.{1,10}слово2</code></td>
        <td>Два слова рядом (от 1 до 10 символов между ними)</td>
      </tr>
      <tr>
        <td><code>(вариант1|вариант2|вариант3)</code></td>
        <td>Любое из нескольких слов</td>
      </tr>
      <tr>
        <td><code>\\bслово\\b</code></td>
        <td>Точное слово (не часть другого)</td>
      </tr>
      <tr>
        <td><code>корень\\w{1,5}</code></td>
        <td>Слово с разными окончаниями</td>
      </tr>
      <tr>
        <td><code>[аa][оo][еe]</code></td>
        <td>Буквы в разных раскладках</td>
      </tr>
    </table>
  </section>

  <p class="wiki-footer-note">
    Начните с простых триггеров. Для сложных конструкций используйте визуальный конструктор связанных триггеров.
  </p>
</div>
`;
